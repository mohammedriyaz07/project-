# MongoDB Configuration
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=secure_password_here
MONGO_DATABASE=crudapp

# Backend Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
NODE_ENV=production

# Optional: Database URL (alternative to individual MongoDB settings)
# MONGO_URI=mongodb://admin:secure_password_here@mongodb:27017/crudapp?authSource=admin

# Application Ports (for reference)
# BACKEND_PORT=3000
# FRONTEND_PORT=80

# CORS Settings (if needed in backend)
# CORS_ORIGIN=http://localhost

# Email Configuration (if your app has email features)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
